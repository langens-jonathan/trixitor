
import { dropDown } from 'trixitor/helpers/drop-down';
import { module, test } from 'qunit';

module('Unit | Helper | drop down');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dropDown([42]);
  assert.ok(result);
});

